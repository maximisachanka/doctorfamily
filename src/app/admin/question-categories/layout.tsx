export const metadata = {
  title: 'Категории вопросов | Админ-панель',
  description: 'Управление категориями вопросов и ответов',
};

export default function QuestionCategoriesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
