# SMAlert - Ультра-современная система уведомлений

Невероятно красивая и современная система уведомлений с потрясающими 3D-анимациями для SmartMedical.

## Возможности

✨ **Дизайн нового поколения:**
- 4 типа алертов: success, error, warning, info
- Яркие градиентные фоны с эффектом глубины
- Анимированные частицы в фоне
- Пульсирующие иконки с эффектом свечения
- Динамические градиентные оверлеи
- Shine эффект на прогресс баре
- 3D-трансформации при наведении
- Вращающаяся кнопка закрытия

🎭 **Продвинутые анимации:**
- Плавный вход с spring-анимацией слева
- Элегантный выход влево
- 8 анимированных частиц для каждого алерта
- Пульсирующее свечение иконки
- Движущиеся градиенты
- Анимированный shine на progress bar

🎨 **Визуальные эффекты:**
- Backdrop blur для глубины
- Box shadows с цветным свечением
- Масштабирование при hover
- Ring borders на элементах
- Drop shadows на тексте

## Использование

### Базовый пример

```tsx
'use client';

import { useAlert } from '@/components/common/SMAlert';

export function MyComponent() {
  const alert = useAlert();

  const handleSuccess = () => {
    alert.success('Операция выполнена успешно!', 'Успех');
  };

  const handleError = () => {
    alert.error('Что-то пошло не так', 'Ошибка');
  };

  const handleWarning = () => {
    alert.warning('Будьте осторожны!', 'Предупреждение');
  };

  const handleInfo = () => {
    alert.info('Полезная информация', 'Информация');
  };

  return (
    <div>
      <button onClick={handleSuccess}>Показать успех</button>
      <button onClick={handleError}>Показать ошибку</button>
      <button onClick={handleWarning}>Показать предупреждение</button>
      <button onClick={handleInfo}>Показать информацию</button>
    </div>
  );
}
```

### Примеры из проекта

#### Авторизация (Header.tsx:212)
```tsx
const alert = useAlert();

const handleLogin = async (credentials) => {
  try {
    const result = await signIn("credentials", credentials);

    if (result?.error) {
      alert.error("Неверный логин или пароль", "Ошибка входа");
    } else if (result?.ok) {
      alert.success("Вы успешно вошли в систему!", "Добро пожаловать");
    }
  } catch (err) {
    alert.error("Ошибка при входе. Попробуйте позже.", "Ошибка");
  }
};
```

#### Загрузка файлов (SMImageUpload.tsx:72)
```tsx
const alert = useAlert();

const handleUpload = async (files) => {
  try {
    // ... загрузка файлов ...

    alert.success(
      files.length === 1
        ? 'Файл успешно загружен!'
        : `Загружено файлов: ${files.length}`,
      'Успешная загрузка'
    );
  } catch (err) {
    alert.error(err.message, 'Ошибка загрузки');
  }
};
```

## API

### useAlert Hook

#### success(message: string, title?: string, duration?: number)
Показать уведомление об успехе

#### error(message: string, title?: string, duration?: number)
Показать уведомление об ошибке

#### warning(message: string, title?: string, duration?: number)
Показать предупреждение

#### info(message: string, title?: string, duration?: number)
Показать информационное уведомление

#### showAlert(options: ShowAlertOptions)
Универсальная функция для показа уведомлений

#### hideAlert(id: string)
Закрыть конкретное уведомление по ID

#### clearAll()
Закрыть все уведомления

## Настройка

### Параметры по умолчанию
- **Длительность**: 5000ms (5 секунд)
- **Позиция**: bottom-left (левый нижний угол)
- **Анимация входа**: spring + slide from left + scale
- **Анимация выхода**: fade + slide to left + scale
- **Размер**: min-width 360px, max-width 448px (md)
- **Цветовая схема**: Яркие градиенты с эффектом глубины

### Кастомизация

```tsx
// Уведомление с кастомной длительностью
alert.success('Сохранено!', 'Успех', 10000); // 10 секунд

// Уведомление без автозакрытия
alert.showAlert({
  type: 'info',
  message: 'Важная информация',
  title: 'Внимание',
  duration: 0, // не закрывается автоматически
});
```

## Анимации

Система использует framer-motion для создания невероятно плавных и красивых анимаций:

### Основные анимации:
1. **Вход алерта**:
   - Spring-анимация с параметрами stiffness: 260, damping: 20
   - Slide from left (translateX: -100 → 0)
   - Scale: 0.9 → 1
   - Opacity: 0 → 1

2. **Выход алерта**:
   - Slide to left (translateX: 0 → -100)
   - Scale: 1 → 0.9
   - Duration: 300ms

3. **Иконка**:
   - Вращение от -180° до 0°
   - Spring-анимация с stiffness: 300
   - Пульсирующее свечение (scale: 1 → 1.2 → 1)
   - Бесконечная анимация с duration: 2s

4. **Частицы** (8 штук):
   - Случайное позиционирование
   - Движение вверх с затуханием
   - Scale: 0 → 1 → 0
   - Opacity: 0 → 1 → 0
   - Бесконечное повторение

5. **Градиентный оверлей**:
   - Перемещение radial-gradient
   - Duration: 4s, бесконечно
   - Создает эффект живого фона

6. **Progress bar shine**:
   - Анимированный градиент
   - Движение слева направо
   - Duration: 1.5s, бесконечно

7. **Hover эффекты**:
   - Масштабирование алерта: 1 → 1.02
   - Shine sweep: translateX -100% → 100%
   - Кнопка закрытия: вращение на 90° + scale 1.1

8. **Текст**:
   - Fade in + slide down
   - Delay для последовательного появления

## Структура файлов

```
SMAlert/
├── SMAlert.tsx          # Основной компонент уведомления
├── AlertContainer.tsx   # Контейнер для стека уведомлений
├── AlertProvider.tsx    # Context Provider и useAlert hook
├── index.ts            # Экспорты
└── README.md           # Документация
```

## Типы

```typescript
type AlertType = 'success' | 'error' | 'warning' | 'info';

interface AlertProps {
  type: AlertType;
  title?: string;
  message: string;
  duration?: number;
  onClose?: () => void;
  closable?: boolean;
  className?: string;
}
```

## Требования

- React 19+
- framer-motion ^12.0.0
- lucide-react ^0.548.0
- Tailwind CSS ^4.0.0
